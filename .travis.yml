env:
  CODECLIMATE_REPO_TOKEN: 191772259dafaf213aeef42256697f88258ad205a4196eee96dfc10a34776a3f

language: node_js

node_js:
  - 7

before_install:
# package-lock.json was introduced in npm@5
- npm install -g npm
- npm install -g greenkeeper-lockfile@1

before_script: greenkeeper-lockfile-update

script:
  - npm run test

after_script: greenkeeper-lockfile-upload

notifications:
  email:
    on_failure: change

after_success: 'npm run coveralls'

cache:
  yarn: true
  directories:
    - node_modules
